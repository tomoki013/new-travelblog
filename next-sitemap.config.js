/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://tomokichidiary.netlify.app/', // あなたのサイトのURLに置き換えてください
    generateRobotsTxt: true, // robots.txt の生成を有効にする場合
    // 他のオプション（例：exclude, changefreq, priority など）も必要に応じて追加できます
}